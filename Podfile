# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'moviebase' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for moviebase

pod 'Alamofire', '~> 5.2'
pod 'ObjectMapper', '~> 3.5'
pod 'Kingfisher', '~> 6.0'
pod 'Parchment', '~> 3.0'

  target 'moviebaseTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'moviebaseUITests' do
    # Pods for testing
  end

end
